{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    { "src": "/api/v1/(.*)", "dest": "/routes/index.js" },
    { "src": "/(.*)", "dest": "/app.js" }
  ]
}
